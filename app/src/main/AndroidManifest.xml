<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nhathuy.app_orderfood">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".Activity.DangNhapActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.DangKyActivity" />
        <activity android:name=".Activity.TrangChuActivity" />
        <activity android:name=".Activity.ThemThucDonActivity" />
        <activity android:name=".Activity.ThanhToanActivity" />

        <!-- set các activity này làm PopUp -->
        <activity
            android:name=".Activity.ThemBanAnActivity"
            android:label="@string/thembanan"
            android:theme="@style/PopUpActivity" />

        <activity
            android:name=".Activity.ThemLoaiThucDonActivity"
            android:label="@string/themloaithucdon"
            android:theme="@style/PopUpActivity" />

        <activity
            android:name=".Activity.SoLuongActivity"
            android:label="@string/soluong"
            android:theme="@style/PopUpActivity" />

        <activity android:name=".Activity.SuaBanAnActivity"
            android:theme="@style/PopUpActivity"/>
    </application>

</manifest>
